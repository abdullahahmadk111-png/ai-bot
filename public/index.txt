<!DOCTYPE html>
<html>
<head>
    <title>My AI Chatbot</title>
    <link rel="stylesheet" href="style.css">
</head><body>

<!-- Logo on top-left of the page -->
<img src="Hawk.exe.png" id="page-logo" alt="Logo">

<div id="chat-container">
    <h2>My AI Chatbot</h2>

    <div id="chat"></div>

    <div id="input-area">
        <input id="msg" placeholder="Type message..." />
        <button onclick="send()">Send</button>
    </div>
</div>

<script>
async function send() {
    const message = document.getElementById("msg").value;
    if (!message) return;

    // Append user message
    const chatBox = document.getElementById("chat");
    chatBox.innerHTML += `<p class="user"><b>You:</b> ${message}</p>`;
    chatBox.scrollTop = chatBox.scrollHeight;

    // Send to server
    const res = await fetch("http://127.0.0.1:5000/chat", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ message })
    });

    const data = await res.json();

    // Append bot reply
    if (data.reply) {
        chatBox.innerHTML += `<p class="bot"><b>Bot:</b> ${data.reply}</p>`;
    } else {
        chatBox.innerHTML += `<p class="bot"><b>Error:</b> ${data.error}</p>`;
    }

    chatBox.scrollTop = chatBox.scrollHeight;
    document.getElementById("msg").value = "";
}

// **Trigger send on Enter key**
document.getElementById("msg").addEventListener("keydown", function(e) {
    if (e.key === "Enter") {
        send();
    }
});
</script>
</body>
</html>